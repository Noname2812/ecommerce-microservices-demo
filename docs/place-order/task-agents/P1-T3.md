Checklist:
Schema:

 EF Core entity OutboxMessage với các fields: Id, Type, Payload, Status, CreatedAt, ProcessedAt, RetryCount, LastError
 Migration: tạo bảng OutboxMessages
 Index trên (Status, CreatedAt)

IOutboxWriter:

 Interface IOutboxWriter với method AddAsync(string type, object payload)
 Implementation: serialize payload thành JSON, INSERT vào OutboxMessages
 KHÔNG tự commit transaction — caller tự quản lý transaction

OutboxWorker:

 BackgroundService (IHostedService) hoặc Hangfire job
 Poll mỗi 5 giây, batch tối đa 50 messages PENDING
 Với mỗi message: publish qua MassTransit, đánh dấu PROCESSED
 Nếu publish fail: tăng RetryCount, log warning
 Sau RetryCount >= 5: đánh dấu FAILED, log error + alert

Test:

 Unit test IOutboxWriter: gọi AddAsync → row trong DB với Status=PENDING
 Unit test OutboxWorker: mock broker, verify PROCESSED sau khi publish
 Unit test OutboxWorker: broker fail → RetryCount tăng, không mất message