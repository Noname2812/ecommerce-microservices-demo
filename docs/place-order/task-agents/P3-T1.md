Database Schema & Redis Setup
Checklist:

 EF Core entity Coupon: Code (PK), DiscountType, DiscountValue, TotalQuota (nullable), UsedQuota, MinOrderValue, ValidFrom, ExpiresAt, IsActive
 EF Core entity CouponClaim: Id, CouponCode, UserId, OrderIdempotencyKey, DiscountAmount, Status, ExpiresAt, CreatedAt, ReleasedAt
 EF Core entity ProcessedEvent (tương tự Inventory)
 Migrations
 Index: CouponClaims(CouponCode, UserId) WHERE Status='CLAIMED' — unique partial index
 Index: CouponClaims(Status, ExpiresAt)
 Redis setup: IConnectionMultiplexer DI registration
 Redis key conventions (document trong code):

coupon:{code}:user:{userId} — per-user lock, TTL 15min
coupon:{code}:quota — global counter


 Seed data: ít nhất 5 coupons với các trường hợp khác nhau (unlimited, limited quota, expired)
 Script khởi tạo Redis quota khi seed (sync quota từ DB → Redis)