Structured Logging & Metrics

Checklist:

 Serilog với JSON formatter cho production
 Enrichers: correlationId, userId, service, environment
 Metrics (Prometheus format):

 order_place_total{status} — counter
 order_place_duration_ms — histogram (buckets: 50, 100, 200, 500, 1000ms)
 compensation_triggered_total{reason} — counter
 reservation_ttl_released_total — counter (trong Inventory job)
 coupon_ttl_released_total — counter (trong Coupon job)


 Health check endpoints: /health/live và /health/ready