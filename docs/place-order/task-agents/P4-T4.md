Inventory Reserve Call

Checklist:

 IInventoryClient interface (Refit):

 ReserveAsync(ReserveRequest request) → ReserveResponse
 ReleaseAsync(Guid reservationId) → void


 HttpClientFactory + Refit registration với base URL từ config
 Timeout: 5 giây
 Gọi với idempotencyKey = $"{requestIK}:inv"
 Map responses:

 201 → return ReserveResponse
 409 → throw OutOfStockException(detail)
 4xx khác → throw InventoryValidationException
 5xx / timeout → throw InventoryUnavailableException



Tests:

 201 → ReserveResponse đúng
 409 → OutOfStockException với detail
 Timeout → InventoryUnavailableException