TTL Background Job

⚠️ Deploy trước khi Claim API go live

Checklist:

 Hangfire RecurringJob mỗi 2 phút
 Query: CouponClaims WHERE Status='CLAIMED' AND ExpiresAt < now
 Với mỗi expired claim:

 DEL coupon:{code}:user:{userId} (Redis, idempotent)
 INCR coupon:{code}:quota (nếu TotalQuota != null)
 Status = RELEASED


 Batch tối đa 200 records
 Log số lượng released

Tests:

 Claim hết hạn → released, Redis key xóa, quota tăng
 Claim chưa hết hạn → không bị release