Release API
Endpoint: DELETE /internal/v1/reservations/{reservationId}
Checklist:

 Load Reservation theo Id — nếu không tồn tại → 404
 Nếu Status = RELEASED → trả 200 ngay (idempotent no-op)
 Trong DB transaction:

 Available += quantity, Reserved -= quantity
 Status = RELEASED, ReleasedAt = UtcNow


 Không bao giờ để Available hoặc Reserved âm (constraint + check trong code)

Tests:

 Release thành công
 Release reservation không tồn tại → 404
 Release 2 lần → lần 2 no-op, Available không thay đổi