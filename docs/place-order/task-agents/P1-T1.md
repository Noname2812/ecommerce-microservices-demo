[P1-T1] Message Broker Setup
Owner: A
Est: 4h
Priority: 🔴 CRITICAL — B và C block vào đây
Checklist:

 Chọn RabbitMQ (default)
 Docker Compose file với RabbitMQ + management UI
 Tạo exchanges:

 order.events (fanout)
 inventory.events (direct)
 coupon.events (direct)
 compensation.events (fanout)


 Dead-letter exchange cho mỗi queue
 MassTransit config base (connection string, retry policy)
 Health check: GET /health phải check broker connection
 README: cách chạy local, cách xem RabbitMQ management UI