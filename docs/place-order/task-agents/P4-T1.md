Database Schema
Checklist:

 Entity Order: Id, UserId, Status, ReservationId, CouponClaimId (nullable), IdempotencyKey, PricingSnapshot (JSON), ShippingAddress (JSON), TotalAmount, FinalAmount, CreatedAt
 Migrations
 Index: Orders(IdempotencyKey) UNIQUE
 Index: Orders(UserId, CreatedAt DESC)
 Apply migrations từ P1-T3 (OutboxMessages) và P1-T5 (CompensationOutbox)