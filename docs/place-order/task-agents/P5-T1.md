Polly — Retry + Circuit Breaker

Checklist:

 Polly policy cho IInventoryClient:

 Retry: 3 lần, exponential backoff 100ms → 200ms → 400ms
 Chỉ retry: HttpRequestException, TaskCanceledException, status 5xx
 Không retry: status 4xx (business error)
 Circuit Breaker: OPEN sau 5 lần fail trong 30s, HALF-OPEN sau 10s


 Áp dụng tương tự cho ICouponClient
 Log khi: retry attempt, circuit breaker state change
 Unit test: retry được trigger đúng số lần
 Unit test: circuit breaker mở sau threshold, fast fail khi OPEN