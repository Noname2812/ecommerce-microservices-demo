Consumer — IInventoryReleaseRequested
Checklist:

 MassTransit consumer class implement IConsumer<IInventoryReleaseRequested>
 Subscribe exchange compensation.events
 Logic:

 Check ProcessedEvents theo EventId — đã xử lý → return (skip)
 Gọi Release logic (tái dùng P2-T3)
 INSERT ProcessedEvents(EventId, EventType, ProcessedAt)


 Exception handling: nếu release fail → throw để MassTransit retry
 Config retry policy trên consumer: appsettings `Inventory:Messaging:InventoryReleaseRequested:Retry` (mặc định 3 interval × 5 giây)

Tests:

 Nhận event hợp lệ → reservation released
 Nhận event đã xử lý (duplicate) → no-op, không release 2 lần
 Release fail → exception propagate để retry