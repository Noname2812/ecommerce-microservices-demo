Coupon Claim Call
Checklist:

 ICouponClient interface (Refit)
 Timeout: 5 giây
 Gọi với idempotencyKey = $"{requestIK}:cpn"
 Map responses:

 201 → return ClaimResponse
 409 → throw CouponException(type, detail)
 422 → throw CouponValidationException
 5xx / timeout → throw CouponUnavailableException


 Quan trọng: Mọi exception ở đây đều phải trigger ghi CompensationOutbox cho inventory

Tests:

 201 → ClaimResponse đúng
 409 → exception đúng loại
 Khi exception → CompensationOutbox được ghi