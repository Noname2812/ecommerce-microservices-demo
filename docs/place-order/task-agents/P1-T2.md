Priority: 🔴 CRITICAL — B và C cần để define consumer
Checklist:

 Tạo project Shared.Contracts (class library)
 Define các interfaces sau (chỉ interface, không implementation):

 IOrderCreated — OrderId, UserId, Items[], TotalAmount, CouponCode?, IdempotencyKey, CreatedAt
 IInventoryReserved — ReservationId, OrderIdempotencyKey, Items[], ExpiresAt
 IInventoryReserveFailed — OrderIdempotencyKey, Reason, FailedProductId
 IInventoryReleaseRequested — ReservationId, Reason, CorrelationId
 ICouponClaimed — ClaimId, CouponCode, UserId, DiscountAmount, ExpiresAt
 ICouponClaimFailed — OrderIdempotencyKey, CouponCode, Reason
 ICouponReleaseRequested — ClaimId, Reason, CorrelationId
 IOrderConfirmed — OrderId, UserId, ReservationId, ClaimId?, FinalAmount, ConfirmedAt


 Thêm field string EventId (Guid) vào mỗi interface — dùng cho idempotency consumer
 Publish lên NuGet feed nội bộ (hoặc project reference nếu chưa có feed)
 Viết CHANGELOG.md trong project