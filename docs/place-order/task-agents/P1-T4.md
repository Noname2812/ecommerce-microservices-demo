Checklist:

 Middleware IdempotencyMiddleware cho ASP.NET Core pipeline
 Validate header Idempotency-Key: bắt buộc, phải là UUID v4 format

 Thiếu header → 400 { type: "MISSING_IDEMPOTENCY_KEY" }
 Format sai → 400 { type: "INVALID_IDEMPOTENCY_KEY" }


 Redis key pattern: idempotency:{key}:{serviceId} (tránh conflict giữa services)
 Logic:

 Redis GET → có response cached → trả ngay với đúng status code
 Không có → cho request đi tiếp, capture response
 Sau khi có response: SET Redis TTL 24h


 Chỉ cache response với status code 2xx và 4xx (không cache 5xx)
 Unit test: request trùng key → trả cache, không gọi controller
 Unit test: key khác nhau → xử lý độc lập
 Unit test: thiếu header → 400